---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: postgresdatabaseinstances.database.mettle.io
spec:
  group: database.mettle.io
  names:
    kind: PostgresDatabaseInstance
    listKind: PostgresDatabaseInstanceList
    plural: postgresdatabaseinstances
    singular: postgresdatabaseinstance
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PostgresDatabaseInstance is the Schema for the postgresdatabaseinstances
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PostgresDatabaseInstanceSpec defines the desired state of
              PostgresDatabaseInstance.
            properties:
              db_name:
                description: Foo is an example field of PostgresDatabaseInstance.
                  Edit postgresdatabaseinstance_types.go to remove/update
                type: string
              drop_on_delete:
                default: true
                type: boolean
              rotation_interval:
                default: 0s
                type: string
              userName:
                type: string
            required:
            - db_name
            - userName
            type: object
          status:
            description: PostgresDatabaseInstanceStatus defines the observed state
              of PostgresDatabaseInstance.
            properties:
              message:
                type: string
              phase:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
              secret:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
